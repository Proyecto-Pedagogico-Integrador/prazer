<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Cliente</title>
    <link rel="stylesheet" href="/img/styles/cliente.css">
    <style>
        /* Estilos para hacer el formulario responsive */
        .container.p-4 {
            padding: 2rem !important;
            /* Reducimos el padding en pantallas pequeñas */
            margin: 0 auto !important;
            /* Centramos el contenedor */
            max-width: 100%;
            /* Aseguramos que el contenedor no se desborde */
        }

        .card {
            margin: 0 auto;
            /* Centramos la tarjeta */
            max-width: 400px;
            /* Limitamos el ancho de la tarjeta */
        }
    </style>
</head>

<body>
    <div class="container p-4">
        <div class="row" style="position: relative;">

            <span class="text" tabindex="0" data-placement="top" title="Volver" style="display: inline-block">
                <a class="btn btn-outline-success" href="/cliente">
                    <span class="icon text-dark-50">
                        <i class="fas fa-arrow-left"></i>
                    </span>
                    <span class="text"></span>
                </a>
            </span>

            <div class="col-md-4 mx-auto">
                <div class="card">
                    <div class="card-body shadow-lg">
                        <h3 style="text-align: center;">Editar Cliente</h3>
                        <br>
                        <form action="/cliente/edit/{{cliente.id_cliente}}" method="POST">
                            <div class="form-group">
                                <h6 style="text-align: left;">NIT*</h6>
                                <input type="text" name="id_cliente" class="form-control" placeholder="nit"
                                    value="{{cliente.id_cliente}}"
                                    onkeypress="return (event.keyCode >= 48 && event.keyCode <= 57 || event.keyCode == 45)"
                                    minlength="8" maxlength="11" required>
                            </div>
                            <div class="form-group">
                                <h6 style="text-align: left;">Nombre*</h6>
                                <input type="text" name="nombre" class="form-control" placeholder="nombre"
                                    value="{{cliente.nombre}}" maxlength="45" autofocus required>
                            </div>
                            
                            <div class="form-group">
                                <h6 style="text-align: left;">Teléfono*</h6>
                                <input type="text" name="telefono" class="form-control" placeholder="telefono"
                                    value="{{cliente.telefono}}" lass="form-control"
                                    onkeypress="return (event.keyCode >= 48 && event.keyCode <= 57)" minlength="7"
                                    maxlength="10" required>
                            </div>
                            <div class="form-group">
                                <h6 style="text-align: left;">Dirección*</h6>
                                <input type="text" name="direccion" class="form-control" placeholder="direccion"
                                    value="{{cliente.direccion}}" minlength="10" maxlength="60" required>
                            </div>
                            <div class="form-group">
                                <button class="btn btn-success btn-block">
                                    Guardar
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <footer>
    <h5>Teléfono</h5>
    <h6>312-695-0222</h6>
    <h5>Dirección</h5>
    <h6>Calle 82 #50A - 48. Santa María, Itagüí</h6>
  </footer>
</body>

</html>