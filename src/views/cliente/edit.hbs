<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Cliente</title>
    <link rel="stylesheet" href="/img/styles/cliente.css">
    <style>
        /* Estilos para hacer el formulario responsive */
        .container.p-4 {
            padding: 2rem !important; /* Reducimos el padding en pantallas pequeñas */
            margin: 0 auto !important; /* Centramos el contenedor */
            max-width: 100%; /* Aseguramos que el contenedor no se desborde */
        }
        
        .card {
            margin: 0 auto; /* Centramos la tarjeta */
            max-width: 400px; /* Limitamos el ancho de la tarjeta */
        }
    </style>
</head>
<body>
<div class="container p-4">
    <div class="row" style="position: relative;">

        <span class="text" tabindex="0" data-placement="top" title="Volver" style="display: inline-block">
            <a class="btn btn-outline-success" href="/cliente">
                <span class="icon text-dark-50">
                    <i class="fas fa-arrow-left"></i>
                </span>
                <span class="text"></span>
            </a>
        </span>

        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body shadow-lg">
                    <h3 style="text-align: center;">Editar Cliente</h3>
                    <br>
                    <form action="/cliente/edit/{{cliente.id_cliente}}" method="POST">
                        <div class="form-group">
                            <input type="text" name="nombre" class="form-control" placeholder="nombre"
                                value="{{cliente.nombre}}" autofocus>
                        </div>
                        <div class="form-group">
                            <input type="text" name="nit" class="form-control" placeholder="nit"
                                value="{{cliente.nit}}">
                        </div>
                        <div class="form-group">
                            <input type="text" name="telefono" class="form-control" placeholder="telefono"
                                value="{{cliente.telefono}}">
                        </div>
                        <div class="form-group">
                            <input type="text" name="direccion" class="form-control" placeholder="direccion"
                                value="{{cliente.direccion}}">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>
